name: CI/CD Pipeline - Sistema Biblioteca

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  backend_tests:
    name: Backend Tests & Lint
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend-node
    steps:
      - name: Clonar Repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'backend-node/package-lock.json'

      - name: Instalar Dependencias Backend
        run: npm ci

      - name: Lint del código Backend
        run: npm run lint

      - name: Ejecutar Pruebas Backend
        run: npm test

      - name: Generar Coverage Report
        run: npm run test:coverage

      - name: Verificar Cobertura >=95%
        run: |
          COVERAGE=$(cat coverage/coverage-summary.json | jq '.total.lines.pct')
          if (( $(echo "$COVERAGE < 95" | bc -l) )); then
            echo "Cobertura insuficiente: $COVERAGE%"
            exit 1
          fi

  frontend_tests:
    name: Frontend Build & Tests  
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend-angular/frontend-angular
    steps:
      - name: Clonar Repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'frontend-angular/frontend-angular/package-lock.json'

      - name: Instalar Dependencias Frontend
        run: npm ci

      - name: Build Frontend
        run: npm run build

      - name: Tests Frontend (si están configurados)
        run: npm test -- --watch=false --browsers=ChromeHeadless
        continue-on-error: true

  performance_tests:
    name: Performance Tests
    runs-on: ubuntu-latest
    needs: [backend_tests]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    defaults:
      run:
        working-directory: backend-node
    steps:
      - name: Clonar Repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'backend-node/package-lock.json'

      - name:  Instalar Dependencias
        run: npm ci

      - name: Instalar K6
        run: |
          sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys C5AD17C747E3415A3642D57D77C6C491D6AC1D69
          echo "deb https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update
          sudo apt-get install k6

      - name: Iniciar Backend para Performance Tests
        run: npm start &
        
      - name: Esperar que el Backend esté listo
        run: sleep 5
        
      - name: Ejecutar Performance Tests
        run: k6 run prueba-carga-rendimiento.js --quiet
        continue-on-error: true  # Performance tests son informativos, no bloquean el pipeline